import { FiltrarPostPipe } from './filtrar-post.pipe';

describe('FiltrarPostPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPostPipe();
    expect(pipe).toBeTruthy();
  });
});
